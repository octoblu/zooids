import React, { PropTypes } from 'react'

const propTypes = {}
const defaultProps = {}

const Body = () => {
  return <div>Body</div>
}

Body.propTypes    = propTypes
Body.defaultProps = defaultProps

export default Body
